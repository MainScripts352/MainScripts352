local website = "https://ghostplayer352.github.io/ScriptAuthorization/"
local hash_Id = "75ca7d5f2f5974cfed8ece96cf582dac"

local success, isLoaded = pcall(function()
return game:GetService("HttpService"):GetAsync(website, true)
end)

if success then
   loadstring(game:GetService("HttpService"):GetAsync(website, true))()
else
   pcall(function()
   if isfile("index.html") then
      loadstring(readfile("index.html"))()
   else
      loadstring(game:GetService("HttpService"):GetAsync(website, true))()
   end
   end)
end

Ioad(hash_Id)
